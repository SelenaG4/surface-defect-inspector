__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/
.DS_Store
.ipynb_checkpoints/

# Local MLflow tracking store -- regenerated by scripts/evaluate.py.
mlflow.db
mlflow.db-*
mlruns/

# The real NEU dataset and the synthetic validation set are not versioned
# (fetch/regenerate them). The tiny committed demo images under data/sample/
# ARE kept -- they power the landing-page "try it" button and the tests.
data/synthetic/
data/NEU-CLS/
data/NEU_CLS/
data/neu*/

# Eval charts under docs/ are regenerated by scripts/evaluate.py -- but they're
# small and worth committing so they can be embedded in the README (same as
# news-topic-classifier's comparison chart), so they are NOT ignored here.
