[pytest]
# Put the repo root on sys.path so `from app... import ...` works under the bare
# `pytest` command CI runs (pytest's default import mode otherwise only adds the
# test file's own directory). Included from day one here because an earlier
# project in this portfolio hit exactly this as a CI-only ModuleNotFoundError.
pythonpath = .
